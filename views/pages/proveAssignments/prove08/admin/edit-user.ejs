<%- include('../../includes/head.ejs') %>
    <link rel="stylesheet" href="/stylesheets/forms.css">
    <link rel="stylesheet" href="/stylesheets/auth.css">
</head>

<body>
   <%- include('../../includes/prove08/navigation.ejs') %>

    <main>
        <% if (errorMessage) { %>
            <div class="user-message user-message--error"><%= errorMessage %></div>
        <% } %>
        <form class="login-form" action="../../../../proveAssignments/08/admin/update-user" method="POST">
            <div class="form-control">
                <label for="first">First Name</label>
                <input type="text" name="first" id="first" value="<%= first %>">
            </div>
            <div class="form-control">
                <label for="last">Last Name</label>
                <input type="text" name="last" id="last" value="<%= last %>">
            </div>
            <div class="form-control">
                <label for="email">Email Address</label>
                <input type="text" name="email" id="email" value="<%= email %>">
            </div>
            <div class="form-control">
                <label for="userType">User Type</label>
                <select name="userType" id="userType" value='<%= user.userType %>'>
                    <option value="admin" <% if (user.userType === 'admin') { %> selected <% } %>>Admin</option>
                    <option value="user" <% if (user.userType === 'user') { %> selected <% } %>>User</option>
                </select>
            </div>
            <input type="hidden" value="<%= userId %>" name="userId">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <input type="hidden" value="<%= user %>" name="user">
            <button class="btn btn_inline" type="submit">Update User</button>
        </form>
        <form class="login-form" action="../../../../proveAssignments/08/admin/delete-user" method="POST">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <input type="hidden" value="<%= userId %>" name="userId">
            <input type="hidden" value="<%= user %>" name="user">
            <button class="btn danger btn_inline" type="submit">Delete User</button>
        </form>
    </main>
<%- include('../../includes/end.ejs') %>